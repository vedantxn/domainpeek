node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Local secrets — never commit. CI reads from GitHub Actions secrets, not this file.
.env
.env.local

# Launch draft — keep local, not part of the repo.
HN_LAUNCH.md
